/**
 * Deprecated SimpleFooter.
 * Kept as an empty shell to avoid accidental rendering of the old dark footer.
 * Use RichFooter instead (src/components/sections/RichFooter.tsx).
 */
export function SimpleFooter() {
  return null;
}

export default SimpleFooter;