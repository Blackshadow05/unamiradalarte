---
import '../styles/globals.css';
import GaleriaUploadForm from '../components/GaleriaUploadForm';
import GaleriaTable from '../components/GaleriaTable';
import AdminObrasStats from '../components/AdminObrasStats';

// Página de administración de obras - Solo accesible desde dashboard
const title = "Administrar Obras - Dashboard";
const description = "Panel de administración para gestionar las obras de arte";

// Verificar si viene del dashboard (opcional - para mayor seguridad)
const referer = Astro.request.headers.get('referer');
// const isDashboardReferer = referer?.includes('/dashboard');
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="description" content={description} />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <title>{title}</title>
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  

</head>

<body class="bg-gradient-to-br from-indigo-50 via-purple-50 to-pink-50 min-h-screen font-inter relative overflow-x-hidden">
  <!-- Decorative background elements -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute -top-40 -right-40 w-80 h-80 bg-gradient-to-br from-purple-400/20 to-pink-400/20 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute -bottom-40 -left-40 w-80 h-80 bg-gradient-to-br from-blue-400/20 to-indigo-400/20 rounded-full blur-3xl animate-pulse" style="animation-delay: 2s;"></div>
    <div class="absolute top-1/3 right-1/4 w-64 h-64 bg-gradient-to-br from-emerald-400/10 to-teal-400/10 rounded-full blur-3xl animate-pulse" style="animation-delay: 4s;"></div>
  </div>
  <!-- Header -->
  <header class="bg-white/90 backdrop-blur-xl border-b border-purple-200/30 sticky top-0 z-50 shadow-lg shadow-purple-500/5">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <!-- Logo y Breadcrumb -->
        <div class="flex items-center space-x-4 animate-fade-in">
          <div class="w-12 h-12 bg-gradient-to-br from-purple-500 via-pink-500 to-rose-500 rounded-2xl flex items-center justify-center shadow-xl shadow-purple-500/25 hover:shadow-2xl hover:shadow-purple-500/40 transition-all duration-300 hover:scale-105">
            <svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
            </svg>
          </div>
          <div>
            <nav class="flex items-center space-x-2 text-sm font-medium">
              <a href="/dashboard" class="text-purple-600 hover:text-purple-800 transition-colors flex items-center">
                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z"></path>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 5a2 2 0 012-2h4a2 2 0 012 2v2H8V5z"></path>
                </svg>
                Dashboard
              </a>
              <span class="text-purple-400">→</span>
              <span class="text-gray-900 font-semibold">Administrar Obras</span>
            </nav>
            <h1 class="text-2xl font-bold bg-gradient-to-r from-purple-600 via-pink-600 to-rose-600 bg-clip-text text-transparent">Gestión de Obras</h1>
          </div>
        </div>

        <!-- Actions -->
        <div class="flex items-center space-x-4">
          <a 
            href="#upload-form" 
            class="inline-flex items-center px-6 py-3 text-sm font-bold text-white bg-gradient-to-r from-purple-600 via-pink-600 to-rose-600 rounded-2xl hover:from-purple-700 hover:via-pink-700 hover:to-rose-700 transition-all duration-300 shadow-xl shadow-purple-500/25 hover:shadow-2xl hover:shadow-purple-500/40 transform hover:scale-105"
          >
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
            ✨ Nueva Obra
          </a>
          <a 
            href="/dashboard" 
            class="inline-flex items-center px-6 py-3 text-sm font-medium text-purple-700 bg-white/80 border border-purple-200 rounded-2xl hover:bg-purple-50 hover:border-purple-300 transition-all duration-300 shadow-lg shadow-purple-500/10 hover:shadow-xl hover:shadow-purple-500/20 backdrop-blur-sm"
          >
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
            </svg>
            Volver al Dashboard
          </a>
        </div>
      </div>
    </div>
  </header>

  <!-- Main Content -->
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 relative z-10">
    <!-- Page Header -->
    <div class="mb-12 animate-slide-in-from-top">
      <div class="text-center mb-8">
        <h2 class="text-5xl font-bold text-gray-900 mb-4 leading-tight">
          Administrar 
          <span class="bg-gradient-to-r from-purple-600 via-pink-600 to-rose-600 bg-clip-text text-transparent animate-pulse">
            Obras de Arte
          </span>
        </h2>
        <div class="w-32 h-1 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full mx-auto mb-4"></div>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto font-light">
          Gestiona tu colección de obras de arte desde este panel 
          <span class="text-purple-600 font-medium">profesional y moderno</span>
        </p>
      </div>
    </div>

    <!-- Estadísticas -->
    <AdminObrasStats client:only="react" />
    
    <!-- Formulario de Subida -->
    <div id="upload-form" class="mb-16 animate-fade-in" style="animation-delay: 0.2s;">
      <GaleriaUploadForm client:only="react" />
    </div>

    <!-- Tabla de Obras -->
    <div class="animate-fade-in" style="animation-delay: 0.4s;">
      <GaleriaTable client:only="react" />
    </div>
  </main>
  </main>

  <!-- Footer -->
  <footer class="mt-20 border-t border-purple-200/30 bg-gradient-to-r from-white/80 via-purple-50/50 to-pink-50/50 backdrop-blur-xl relative z-10">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <div class="text-center">
        <div class="flex items-center justify-center mb-4">
          <div class="w-8 h-8 bg-gradient-to-br from-purple-500 to-pink-500 rounded-lg flex items-center justify-center mr-3">
            <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
            </svg>
          </div>
          <span class="text-lg font-bold bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">Una mirada al arte</span>
        </div>
        <p class="text-gray-600 font-medium">&copy; 2024 Panel de Administración - Gestión de Obras</p>
        <div class="mt-4 flex items-center justify-center space-x-6 text-sm text-gray-500">
          <span>🎨 Admin Panel v2.0</span>
          <span>•</span>
          <span>📊 24 obras gestionadas</span>
          <span>•</span>
          <span class="text-green-600 font-medium">🟢 Sistema Activo</span>
        </div>
      </div>
    </div>
  </footer>
</body>
</html>