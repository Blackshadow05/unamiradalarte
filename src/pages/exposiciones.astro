---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import type { Exhibition } from '../types';

// Datos de ejemplo de exposiciones
const exhibitions: Exhibition[] = [
  {
    id: '1',
    title: 'Colores del Alma',
    description: 'Una exploración íntima de las emociones humanas a través del color y la forma. Esta exposición presenta 25 obras que capturan la esencia de los sentimientos más profundos.',
    startDate: '2024-03-15',
    endDate: '2024-05-20',
    location: 'Galería Arte Contemporáneo, Madrid',
    artworks: ['1', '2', '3', '4', '5'],
    featured: true
  },
  {
    id: '2',
    title: 'Reflexiones Urbanas',
    description: 'La vida en la ciudad vista a través de una perspectiva artística única. Obras que reflejan la complejidad y belleza de la vida urbana contemporánea.',
    startDate: '2023-09-10',
    endDate: '2023-11-15',
    location: 'Centro Cultural Reina Sofía, Madrid',
    artworks: ['3', '6'],
    featured: true
  },
  {
    id: '3',
    title: 'Naturaleza Abstracta',
    description: 'Una interpretación personal de la naturaleza a través de formas abstractas y colores orgánicos que evocan la esencia del mundo natural.',
    startDate: '2023-05-20',
    endDate: '2023-07-30',
    location: 'Museo de Arte Moderno, Barcelona',
    artworks: ['6', '4'],
    featured: false
  }
];

const formatDate = (dateString: string) => {
  return new Intl.DateTimeFormat('es-ES', {
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  }).format(new Date(dateString));
};
---

<Layout 
  title="Exposiciones - Una mirada al arte"
  description="Descubre las exposiciones pasadas y futuras donde se han presentado las obras de 'Una mirada al arte'. Un recorrido por la trayectoria expositiva de la artista."
>
  <Header />
  
  <main class="pt-16">
    <!-- Hero Section -->
    <section class="bg-art-gradient py-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h1 class="font-display text-5xl md:text-6xl font-bold text-white mb-6 fade-in">
          Exposiciones
        </h1>
        <p class="text-xl text-white/90 max-w-3xl mx-auto slide-in-from-bottom">
          Un recorrido por las exposiciones donde mis obras han cobrado vida, conectando con el público y creando experiencias artísticas memorables.
        </p>
      </div>
    </section>
    
    <!-- Exhibitions Section -->
    <section class="py-20 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="space-y-16">
          {exhibitions.map((exhibition, index) => (
            <article class={`grid grid-cols-1 lg:grid-cols-2 gap-12 items-center ${index % 2 === 1 ? 'lg:grid-flow-col-dense' : ''}`}>
              <!-- Image -->
              <div class={`${index % 2 === 1 ? 'lg:col-start-2' : ''} slide-in-from-${index % 2 === 0 ? 'left' : 'right'}`}>
                <div class="relative">
                  <div class="aspect-[4/3] bg-gradient-to-br from-primary-100 to-secondary-100 rounded-2xl overflow-hidden art-shadow">
                    <div class="w-full h-full flex items-center justify-center">
                      <div class="text-center">
                        <div class="w-20 h-20 bg-primary-200 rounded-full mx-auto mb-4 flex items-center justify-center">
                          <svg class="w-10 h-10 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                          </svg>
                        </div>
                        <p class="text-primary-600 font-medium">Exposición</p>
                        <p class="text-primary-500 text-sm">{exhibition.title}</p>
                      </div>
                    </div>
                  </div>
                  
                  <!-- Status Badge -->
                  <div class="absolute top-4 right-4">
                    <span class={`px-3 py-1 rounded-full text-xs font-semibold ${
                      new Date(exhibition.endDate) > new Date() 
                        ? 'bg-green-100 text-green-800' 
                        : 'bg-gray-100 text-gray-800'
                    }`}>
                      {new Date(exhibition.endDate) > new Date() ? 'Activa' : 'Finalizada'}
                    </span>
                  </div>
                </div>
              </div>
              
              <!-- Content -->
              <div class={`${index % 2 === 1 ? 'lg:col-start-1' : ''} slide-in-from-${index % 2 === 0 ? 'right' : 'left'}`}>
                <div class="space-y-6">
                  <div>
                    <h2 class="font-display text-3xl md:text-4xl font-bold text-gray-900 mb-4">
                      {exhibition.title}
                    </h2>
                    
                    <div class="flex flex-wrap gap-4 text-sm text-gray-600 mb-6">
                      <div class="flex items-center">
                        <svg class="w-4 h-4 mr-2 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                        </svg>
                        <span>{formatDate(exhibition.startDate)} - {formatDate(exhibition.endDate)}</span>
                      </div>
                      
                      <div class="flex items-center">
                        <svg class="w-4 h-4 mr-2 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                        </svg>
                        <span>{exhibition.location}</span>
                      </div>
                    </div>
                  </div>
                  
                  <p class="text-lg text-gray-600 leading-relaxed">
                    {exhibition.description}
                  </p>
                  
                  <div class="flex flex-col sm:flex-row gap-4">
                    <a 
                      href="/galeria" 
                      class="inline-flex items-center px-6 py-3 bg-primary-600 text-white font-semibold rounded-full hover:bg-primary-700 transition-all duration-300 hover:scale-105"
                    >
                      <span>Ver Obras</span>
                      <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                      </svg>
                    </a>
                    
                    <a 
                      href="/contacto" 
                      class="inline-flex items-center px-6 py-3 border-2 border-primary-600 text-primary-600 font-semibold rounded-full hover:bg-primary-600 hover:text-white transition-all duration-300"
                    >
                      <span>Más Información</span>
                    </a>
                  </div>
                </div>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>
    
    <!-- CTA Section -->
    <section class="py-20 bg-gray-50">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="font-display text-4xl font-bold text-gray-900 mb-6">
          ¿Interesado en organizar una exposición?
        </h2>
        <p class="text-xl text-gray-600 mb-8">
          Estoy siempre abierta a nuevas oportunidades de exhibir mi trabajo. Si tienes un espacio o una propuesta, me encantaría conocer más detalles.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="/contacto" 
            class="inline-flex items-center px-8 py-4 bg-primary-600 text-white font-semibold rounded-full hover:bg-primary-700 transition-all duration-300 hover:scale-105 art-shadow"
          >
            <span>Proponer Exposición</span>
            <svg class="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </a>
          
          <a 
            href="/galeria" 
            class="inline-flex items-center px-8 py-4 border-2 border-primary-600 text-primary-600 font-semibold rounded-full hover:bg-primary-600 hover:text-white transition-all duration-300"
          >
            <span>Ver Portfolio</span>
          </a>
        </div>
      </div>
    </section>
  </main>
  
  <Footer />
</Layout>